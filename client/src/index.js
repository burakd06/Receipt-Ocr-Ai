import React from 'react';
import ReactDOM from 'react-dom/client';
import AppWithRouter from './App';  
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('Rahat Fiş'));
root.render(
  <React.StrictMode>
    <AppWithRouter /> 
  </React.StrictMode>
);
